use crate::hdl::sv::SvAst;

/// Symbol resolution pass
///
/// TODO No crea IR todavía.
pub(crate) fn resolve_symbols(_ast: &SvAst) -> Result<(), String> {
    // Placeholder: symbol table, scopes, references
    Ok(())
}
